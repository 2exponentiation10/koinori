{
  "name": "koinori",
  "version": "1.0.0",
  "description": "Koinori room reservation service with automatic room assignment and waitlist handling.",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build-client.mjs",
    "dev": "npm run build && node --watch server.js",
    "start": "node server.js",
    "verify": "node --check server.js && node --check public/admin.js && npm run build && node scripts/verify.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "compression": "^1.8.1",
    "ejs": "^5.0.1",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "luxon": "^3.7.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
